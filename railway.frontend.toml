# Railway Configuration for Ghostfolio Agent Frontend (React)

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.frontend"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
